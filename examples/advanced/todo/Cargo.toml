[package]
name = "todo"
version = "0.1.0"
edition = "2021"
license = "ISC"
publish = false

[[bin]]
name = "todo"
path = "src/main.rs"

[dependencies]
gpui = { version = "0.2.2", default-features = false, features = ["font-kit"] }
crepuscularity-gpui = { path = "../../../crates/crepuscularity-gpui", version = "0.5.0" }
