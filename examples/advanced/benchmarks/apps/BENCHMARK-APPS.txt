Checked-in stack fixtures for crepus benchmark (see ../benchmark.toml).

Layout:
  nextjs, svelte, solid, vue, vanilla, angular — Vite / Next / Angular production builds
  electron — electron-vite (Vanilla + TS); re-scaffold: printf '\x1b[B\x1b[B\n' | bunx create-electron-vite@latest electron
             (no bun.lock committed — electron’s postinstall downloads native binaries; run `bun install` locally)
  tauri — create-tauri-app -y -m bun -t vanilla-ts
  plasmo — PlasmoHQ/examples/with-popup (package.json pins plasmo semver)
  leptos — snapshot of leptos-rs/start-axum (no .git)
  avalonia-app — dotnet new avalonia.app (name avoids NuGet "avalonia" clash)

Do not commit node_modules (see .gitignore). Benchmark steps run bun install / dotnet build as needed.
