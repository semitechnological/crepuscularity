[package]
name = "mobile-app-actions"
version = "0.1.0"
edition = "2021"
publish = false

[workspace]

[lib]
crate-type = ["staticlib", "cdylib", "rlib"]

[dependencies]
serde_json = "1"

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.19"
