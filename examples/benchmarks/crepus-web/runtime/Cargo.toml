[package]
name = "crepus_bench_web_runtime"
version = "0.1.0"
edition = "2021"
license = "ISC"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
crepuscularity-web = { path = "../../../../crates/crepuscularity-web", features = ["dom"] }
wasm-bindgen = "=0.2.121"

[workspace]
