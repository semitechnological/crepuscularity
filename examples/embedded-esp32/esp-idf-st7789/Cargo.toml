[package]
name = "embedded-esp32-idf-st7789"
version = "0.1.0"
edition = "2021"
publish = false

# Standalone — build inside an ESP-IDF project (see README.md). Not in the root workspace.

[workspace]

[dependencies]
crepuscularity-embedded = { path = "../../../crates/crepuscularity-embedded", features = [
    "std",
    "esp-idf",
] }
esp-idf-sys = { version = "0.36", default-features = false }
