[package]
name = "crepuscularity-core"
version = "0.3.0"
edition = "2021"
license = "MPL-2.0"
description = "Parser, AST, and expression evaluation for the Crepuscularity .crepus DSL (UNSTABLE; in active development)."
repository = "https://github.com/semitechnological/crepuscularity"
readme = "../../README.md"

[dependencies]
toml = "1"
sha2 = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = "3"
