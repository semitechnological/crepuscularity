[package]
name = "crepuscularity-components"
version = "0.1.0"
edition = "2021"
license = "ISC"
description = "Rust-side component catalog registry for the crepus CLI (synced from plugins/crepuscularity-components)."
repository = "https://github.com/tschk/crepuscularity"
readme = "README.md"
include = ["catalog/**", "src/**", "Cargo.toml", "README.md"]

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
