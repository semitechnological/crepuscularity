[package]
name = "docs_site_renderer"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"

[dependencies]
pulldown-cmark = "0.11"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"

[workspace]
