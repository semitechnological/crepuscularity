# Parallel PR Review — 2026-07-05

30 PRs reviewed across 4 domains by 4 parallel agents.

---

## 🔒 Security (7 PRs)

| PR# | Title | Verdict | Key Findings |
|-----|-------|---------|-------------|
| #177 | Fix path traversal (Go) | ⚠️ | Missing symlink bypass protection (`filepath.EvalSymlinks`). Tests cover basic traversal only. |
| #176 | Fix CREPUS_BUN injection (Python) | ✅ | Clean 5-line validation. Good test coverage. |
| #175 | Fix file read (PHP) | ⚠️ | Root-directory edge case bug. **No tests at all.** No PHP test infra exists. |
| #174 | Fix CREPUS_BUN injection (Ruby) | ✅ | Simplest fix — one-line regex. Strictest (no absolute paths). Correct. |
| #173 | Fix CREPUS_BUN injection (PHP) | ❌ | Over-engineered multi-regex logic with questionable space-hyphen check. **No tests.** Conflicts with #175 on `BIND_BLOCKLIST`. |
| #172 | Fix path traversal (Python) | ✅ | Clean parameter threading, `Path.resolve()` + `is_relative_to()`. Good tests. |
| #162 | Fix path traversal (Ruby) | ✅ | Best test coverage (3 denial + 1 valid, incl. symlink bypass test). |

**Cross-cutting:** Go(#177) & PHP(#175) lack symlink resolution — Python & Ruby get it right. PHP has no test infra. #173 and #175 conflict on `BIND_BLOCKLIST` visibility.

---

## 🧪 Tests (8 PRs)

| PR# | Title | Verdict | Key Findings |
|-----|-------|---------|-------------|
| #152 | Clipboard read_text error path | ❌ | **Wrong.** Empty clipboard returns `Ok("")`, not error. Destructive side-effect (`cb.clear()` erases user clipboard). False-positive risk in headless. |
| #171 | Clipboard read/write error paths | ⚠️ | Correct but over-engineered: 72 lines of subprocess machinery for testing error path of 2-line function. Happy-path skip sufficient. |
| #170 | DOM attr error paths | ✅ | Solid. Tests both `ElementRef` & `StaticElementRef`. WASM-gated. |
| #166 | eval_guest_from_config_file | ✅ | Clean. Missing-file + invalid-TOML. tempfile isolation. |
| #165 | Template functions error paths | ✅ | Correct. `template()` and `draw()` with missing file. Broad assertion but matches. |
| #164 | draw_full error paths | ✅ | Valid parse error triggers `CrepusError::Parse`. Tests method + free fn. |
| #155 | write_ppm error path | ✅ | Minimal. Feature-gated correctly. |
| #148 | Template::from_path error path | ✅ | Uses existing test helpers. Clean. |

**Summary:** 1 reject (#152), 1 over-engineered (#171), 6 clean.

---

## 🧹 Refactor (11 PRs)

| PR# | Title | Net ± | Simpler? | Notes |
|-----|-------|-------|----------|-------|
| #169 | apply_borders_shadows | +8 | ✅ | Genuine concern split. Borders & shadows independent. |
| #168 | JNI string boilerplate | **−29** | ✅ | 12× repeated 6-line pattern → 2 helpers. Textbook DRY. |
| #167 | Native shell build pipeline | +12 | ⚠️ | Marginal. Trivial wrappers around already-readable inner matches. |
| #160 | build_site_wasm | +22 | ✅ | 100+ line monolith → 6 named steps. Worth +22. |
| #158 | draw_frame in benchmark_tui | +17 | ✅ | Standard Ratatui decomposition. Good pattern. |
| #157 | render_seo_head | +12 | ⚠️ | 6-parameter extracted function is itself a smell. Pass `&SiteHead` instead. |
| #156 | write_runtime_assets | +9 | ✅ | Clean extraction. Pipeline fn now readable at a glance. |
| #154 | render_crepus_pages | +11 | ✅ | Loop body extraction. Cache-hit `continue` → `return` clearer. |
| #153 | build_wasm_runtime | +21 | ✅ | Four named steps. Nested blocks → linear Option flow. |
| #163 | run_all_suites | **+419** (+74 real) | ⚠️ | Over-engineered relative to scope. Closure pattern was fine. `.orig` file committed accidentally. |
| #150 | from_manifest_for_browser | +30 | ✅ | 80-line constructor → 7 focused builders. Clean. |

**Summary:** 7 clear wins, 3 mixed, 0 fails. #163 is the outlier — 419 lines of noise.
**#157**: pass `&SiteHead` not 6 params.

---

## ⚡ Performance (4 PRs)

| PR# | Title | Verdict | Findings | ± |
|-----|-------|---------|----------|---|
| #149 | CSS vector allocation | ✅ | Real micro-opt. `with_capacity` avoids realloc. Correct. | +1 |
| #151 | Vec cloning in tree indexing | ✅ | Real alloc reduction. `&mut Vec` push/pop instead of clone per recursion. **⚠️ `.orig` committed.** | ~0 |
| #159 | Subscriber removal HashSet | ✅ | Real opt for many-subscriber case. Vec→HashSet. 16% regression on 1-subscriber (acceptable). Net code reduction. | −2 |
| #161 | Subscriber removal O(N)→O(E) | ✅ | O(N) full scan → O(E) targeted. **Also fixes bug** — missing source-list cleanup. **⚠️ Conflicts with #159** — uses `retain` not `remove`. | +29 |

**Cross-cutting:** #159 and #161 touch same function. #161 uses `retain` which must become `remove` if #159's HashSet lands first. Merge or rebase.

---

## 🔴 Blocker

**PR #152** must be rejected. Empty clipboard returns `Ok("")`, not error. Destructive side-effect. False-positive risk.

## 🟡 Attention needed

- **PR #173**: Over-engineered regex blacklist. No tests. Conflicts with #175. Simplify or reject.
- **PR #163**: 419 lines of structural churn for a lateral readability move. `.orig` committed. Trim.
- **PR #157**: 6-parameter `format_seo_tags`. Pass `&SiteHead` instead.
- **PR #151 / #163**: Remove committed `.orig` files. Add `*.orig` to `.gitignore`.
- **PR #159 / #161**: Order dependency — rebase #161 on #159 and fix `retain` → `remove`.

## 🟢 Clean

22 of 30 PRs are clean and ready to merge after addressing the above issues.

---

*Reviewed 2026-07-05 by 4 parallel agents (1 security, 1 test, 2 refactor/perf).*
