[package]
name = "crepuscularity-components-gpui"
version = "0.1.1"
edition = "2021"
description = "Pure Bayer dither math + palette seeds for crepuscularity-gpui (no gpui dep yet)"
license = "ISC"
publish = false

# Standalone stub — not a member of the crepuscularity workspace.
[workspace]

[lib]
path = "lib.rs"

[lints.rust]
dead_code = "allow"
