name: crepuscularity_components
description: >-
  Multi-target dither charts, primitives, and motion components for Flutter —
  part of the crepuscularity-components registry. General-purpose; no View IR
  dependency.
version: 0.1.0
homepage: https://github.com/tschk/crepuscularity
repository: https://github.com/tschk/crepuscularity
issue_tracker: https://github.com/tschk/crepuscularity/issues
topics:
  - ui
  - charts
  - dither
  - crepuscularity

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
